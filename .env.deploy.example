# Slackbracket deploy credentials
# Copy to .env.deploy and fill in real values. NEVER commit .env.deploy.
#
# NFS (NearlyFreeSpeech) — static site host
NFS_HOST=ssh.nyc1.nearlyfreespeech.net
NFS_USER=aszoke_slackbracket
NFS_PASS=your-nfs-password-here
NFS_REMOTE_DIR=/home/public

# AWS — telemetry Lambda stack
AWS_ACCESS_KEY_ID=AKIA000000000EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_DEFAULT_REGION=us-west-2

# Telemetry endpoint (set after first `infra/telemetry/deploy.sh`)
NEXT_PUBLIC_TELEMETRY_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/track
